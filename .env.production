# `frontend` 下执行 `npm run build` 时由 Vite 读取（envDir 为仓库根目录）。
# 与香港等 HTTPS 站点同源部署：固定为 /api，由 Nginx 反代到本机后端；勿写 localhost（会打进用户浏览器）。
# 若 API 单独子域，再改为完整 URL，例如：https://api.example.hk/api
VITE_API_BASE=/api
